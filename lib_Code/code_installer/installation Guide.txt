==============================
📦 INSTALLATION INSTRUCTIONS
==============================

📥 To Install on Linux/macOS/WSL:
----------------------------------
1. Open your terminal
2. Navigate to the folder containing `run-code-installer.sh`
3. Run the following:

    chmod +x run-code-installer.sh
    ./run-code-installer.sh

🧹 To Uninstall:
----------------
    chmod +x uninstall-code.sh
    ./uninstall-code.sh


📥 To Install on Windows (via .bat file):
-----------------------------------------
1. Right-click `bat ) install-code.bat` and choose **"Run as administrator"**
2. A Command Prompt window will open
3. It will install `code.h` globally and show all progress messages.

⚠️ NOTE:
- This installs `code.h` to `C:\ProgramData\lib_Code`
- It also updates the system INCLUDE path.
- Admin permissions are required!

🧹 To Uninstall on Windows:
----------------------------
1. Right-click `bat ) uninstall-code.bat` and choose **"Run as administrator"**
2. It will:
    - Remove the `lib_Code` folder
    - Clean the INCLUDE environment variable
    - Show output messages in Command Prompt

💡 Alternate (PowerShell only):
-------------------------------
If you'd rather run manually:

    Set-ExecutionPolicy Bypass -Scope Process -Force
    .\install-code.ps1

To uninstall manually:

    Set-ExecutionPolicy Bypass -Scope Process -Force
    .\uninstall-code.ps1

⚠️ NOTE: This only works with an Admin PowerShell Terminal.


===========================
📎 AFTER INSTALLING
===========================

You can now include your header in any C file like this:

    #include <code.h>

And compile with:

    make hello

Works across:
✔️ Windows (MinGW/Clang)
✔️ WSL Ubuntu
✔️ Native Linux/macOS
